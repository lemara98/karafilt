demucs>=4.0
torch>=2.0
torchaudio
websockets
numpy
# Optional: install for BS-Roformer, Mel-Roformer, MDX23C, UVR models
# audio-separator>=0.17
# Optional: install for smart-sync (forced alignment of plain lyrics → synced LRC).
# whisperx pulls faster-whisper, pyannote.audio, transformers, and needs ffmpeg.
# whisperx>=3.1
